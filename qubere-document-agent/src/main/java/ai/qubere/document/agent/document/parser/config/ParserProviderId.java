package ai.qubere.document.agent.document.parser.config;

/** Which provider a deployment is configured to use. Defaults to {@code NONE} so nothing pretends. */
public enum ParserProviderId {
    IBM_DOCLING,
    MOCK,
    NONE
}
