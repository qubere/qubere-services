spring:
  datasource:
    url: ${AGENT_DATASOURCE_URL:jdbc:oracle:thin:@localhost:1521/FREEPDB1}
    username: ${AGENT_DATASOURCE_USERNAME:agents}
    password: ${AGENT_DATASOURCE_PASSWORD:agents}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
